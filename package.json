{
  "name": "ubermanu.github.io",
  "homepage": "https://ubermanu.github.io",
  "private": true,
  "scripts": {
    "build": "eleventy",
    "serve": "eleventy --serve",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@11ty/eleventy": "^1.0.0"
  }
}
